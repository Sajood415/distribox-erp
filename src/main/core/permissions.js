/**
 * Single-user desktop app — session auth only, no RBAC.
 * Login protects IPC; one admin account is seeded at install.
 */
export const AUTH_PUBLIC = undefined;
export const AUTH_REQUIRED = null;
